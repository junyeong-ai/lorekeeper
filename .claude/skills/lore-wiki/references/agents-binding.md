# AGENTS.md Binding

Before any page creation or section editing, read `<wiki>/AGENTS.md` (generated by
`lore schema`; `<wiki>` = `vault.dirs.wiki` from config). It defines every page
type's frontmatter keys, sections, headings, and ownership (machine vs LLM). Never
embed a format spec or heading in this skill — derive everything from AGENTS.md.

If AGENTS.md is missing, tell the user to run `lore schema` first.
