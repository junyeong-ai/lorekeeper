name: Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (e.g. v0.1.0)"
        required: true

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  build:
    name: build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-musl
            os: ubuntu-24.04
            cross: true
            archive: tar.gz
          - target: aarch64-unknown-linux-musl
            os: ubuntu-24.04
            cross: true
            archive: tar.gz
          - target: aarch64-apple-darwin
            os: macos-15
            cross: false
            archive: tar.gz
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            cross: false
            archive: zip

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Install cross
        if: matrix.cross
        run: cargo install cross --locked

      - name: Build (cross)
        if: matrix.cross
        run: cross build --release --locked --target ${{ matrix.target }} -p wi

      - name: Build (native)
        if: '!matrix.cross'
        run: cargo build --release --locked --target ${{ matrix.target }} -p wi

      - name: Resolve version
        id: version
        shell: bash
        run: |
          ref="${{ github.event.inputs.tag || github.ref_name }}"
          version="${ref#v}"
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "tag=v$version" >> "$GITHUB_OUTPUT"

      - name: Package archive
        id: package
        shell: bash
        env:
          VERSION: ${{ steps.version.outputs.version }}
          TARGET: ${{ matrix.target }}
          ARCHIVE_EXT: ${{ matrix.archive }}
        run: |
          set -euo pipefail
          stage="wi-v${VERSION}-${TARGET}"
          mkdir -p "$stage/templates"
          if [ "$ARCHIVE_EXT" = "zip" ]; then
            cp "target/${TARGET}/release/wi.exe" "$stage/"
          else
            cp "target/${TARGET}/release/wi" "$stage/"
          fi
          cp templates/*.md.jinja "$stage/templates/"
          cp config.example.yaml "$stage/"
          cp README.md LICENSE 2>/dev/null "$stage/" || cp README.md "$stage/"
          archive="${stage}.${ARCHIVE_EXT}"
          if [ "$ARCHIVE_EXT" = "zip" ]; then
            (cd "$(dirname "$stage")" && 7z a -tzip "../${archive}" "$(basename "$stage")" >/dev/null)
            mv "../${archive}" "${archive}" 2>/dev/null || true
          else
            tar -czf "$archive" "$stage"
          fi
          if command -v sha256sum >/dev/null 2>&1; then
            sha256sum "$archive" > "${archive}.sha256"
          else
            shasum -a 256 "$archive" > "${archive}.sha256"
          fi
          echo "archive=$archive" >> "$GITHUB_OUTPUT"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.package.outputs.archive }}
          path: |
            ${{ steps.package.outputs.archive }}
            ${{ steps.package.outputs.archive }}.sha256
          if-no-files-found: error

  skill:
    name: package skill
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}
      - name: Resolve version
        id: version
        run: |
          ref="${{ github.event.inputs.tag || github.ref_name }}"
          echo "version=${ref#v}" >> "$GITHUB_OUTPUT"
      - name: Package skill tarballs
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          set -euo pipefail
          # One archive per skill, named `{skill}-skill-v{version}.tar.gz` to match
          # what scripts/install.sh and install.ps1 download. Each contains the
          # skill directory.
          for skill in wiki-ingest wi-process wi-setup; do
            archive="${skill}-skill-v${VERSION}.tar.gz"
            tar -czf "$archive" -C .claude/skills "$skill"
            sha256sum "$archive" > "${archive}.sha256"
          done
      - uses: actions/upload-artifact@v4
        with:
          name: skills
          path: |
            *-skill-v*.tar.gz
            *-skill-v*.tar.gz.sha256
          if-no-files-found: error

  release:
    name: publish release
    needs: [build, skill]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true
      - name: Publish
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ github.event.inputs.tag || github.ref_name }}
        run: |
          set -euo pipefail
          ls -la artifacts
          gh release create "$TAG" \
            --title "$TAG" \
            --generate-notes \
            artifacts/*
