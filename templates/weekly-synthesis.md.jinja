---
id: synthesis-{{ year }}-W{{ "%02d" | format(week) }}
title: "{{ i18n.weekly_synthesis_title }} {{ year }}-W{{ "%02d" | format(week) }}"
created: {{ date }}
labels: {{ labels | tojson }}
period: "{{ year }}-W{{ "%02d" | format(week) }}"
sources_covered: {{ sources | tojson }}
---

# {{ i18n.weekly_synthesis_title }} {{ year }}-W{{ "%02d" | format(week) }}

## {{ i18n.key_themes_this_week }}

{% for theme in themes %}
### {{ loop.index }}. {{ theme.title }}

{{ theme.description }}

{% endfor %}

## {{ i18n.new_concepts }}

{% for concept in new_concepts %}
- [[{{ concept }}]]
{% endfor %}
